'use strict';
/*
angular
  .module('contractionsFeApp', [
    'pregnantService'
  ])
  .config(function($mdDateLocaleProvider) {
    $mdDateLocaleProvider.formatDate = function(date) {
       //return moment(date).format('DD-MM-YYYY');
       return moment(date).format('DD-MM-YYYY');

    };
  });
*/